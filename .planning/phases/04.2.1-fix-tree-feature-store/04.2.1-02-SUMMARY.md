# Phase 04.2.1 Plan 02: build_tree_feature_store() Implementation Summary

**Plan:** 04.2.1-02
**Subsystem:** Tree Feature Store — Core Build Function
**Status:** Complete
**Completed:** 2026-04-07

---

## One-Liner

`build_tree_feature_store()` implemented in `src/features.py` — variance-only filter, no WoE/IV, produces `X_tree_raw.parquet` (307511×211) with zero NaN values and no WoE columns.

---

## Objective

Build the core `build_tree_feature_store()` function: raw (non-WoE) feature matrix with 155+ engineered features, full 307,511 rows, variance filtering only — ready for tree model training.

---

## What Was Built

### Function: `build_tree_feature_store()` (src/features.py:1392)

Pipeline: `engineer_application_features()` → `engineer_secondary_features()` → numeric-only filter → `-999` sentinel fill → variance filter (drop constants + bottom 5% by variance) → save parquet + feature column pkl.

**Key decisions implemented (per CONTEXT.md):**
- D-01: Variance filter only — no IV filtering, no WoE binning
- D-02: Numeric columns only (dtype category/object excluded)
- D-03: Output path `data/processed/X_tree_raw.parquet` (exact)
- D-06: All NaN/inf replaced with `-999` sentinel
- D-07: Renamed from `build_raw_feature_store` (removed IV filter + correlation dedup)

### Companion: `apply_raw_feature_store()` (src/features.py:1481)

Docstring updated to reference `build_tree_feature_store()`. No logic changes — inference path unchanged.

### Artifact: `data/processed/X_tree_raw.parquet`

| Property | Value |
|----------|-------|
| Shape | (307511, 211) |
| NaN count | 0 |
| WoE columns | 0 |
| Disk size | 144 MB |
| Memory size | 485 MB |

### Artifact: `models/raw_feature_columns.pkl`

211 feature column names saved for inference-time alignment.

---

## Self-Check: PASSED

- [x] `build_tree_feature_store()` exists and is callable
- [x] Variance filter only (no IV, no WoE)
- [x] Output shape (307511, 211) — N=211 >= 100 ✓
- [x] Zero NaN values in output
- [x] Output path `data/processed/X_tree_raw.parquet` (exact match)
- [x] `models/raw_feature_columns.pkl` saved
- [x] `apply_raw_feature_store()` docstring updated
- [x] Committed: `feat(04.2.1-02): build_tree_feature_store() — variance-only filter, no WoE/IV`

---

## key-files

### created
- data/processed/X_tree_raw.parquet

### modified
- src/features.py

### unchanged
- models/raw_feature_columns.pkl (updated timestamp)
